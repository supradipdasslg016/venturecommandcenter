services:
  postgres_db:
    image: pgvector/pgvector:pg16
    container_name: strategic_data_core
    environment:
      POSTGRES_USER: strategist_admin
      POSTGRES_PASSWORD: elite_secure_password_2026
      POSTGRES_DB: venture_validation_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: